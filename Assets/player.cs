using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class player : MonoBehaviour {

	//public GameObject plane;

	// Use this for initialization
	void Start () {
		/*var width = plane.GetComponent<Renderer> ().bounds.size.x;
		var height = plane.GetComponent<Renderer> ().bounds.size.z;
		this.transform.position = new Vector3 (-width/2.0f, this.transform.position.y, height/2.0f);*/
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
